import React from "react";
import PropertyDetail from "src/Pages/PropertyDetail.tsx";

type Props = {};

const PropertyDetailPage = (props: Props) => {
  return (
    <div>
      <PropertyDetail />
    </div>
  );
};

export default PropertyDetailPage;
