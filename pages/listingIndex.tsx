import React from "react";
import ListingHome from "src/Pages/propertyHome";

type Props = {};

const PropertyIndex = (props: Props) => {
  return (
    <div>
      <ListingHome />
    </div>
  );
};

export default PropertyIndex;
